# Copy this file to .env.local (already gitignored) and restart the dev
# server to start the app straight into debug mode: free camera + the
# top-left object picker. Default is off; this only affects `npm run dev`
# on your own machine, never the GitHub Pages build.
VITE_DEBUG=true
